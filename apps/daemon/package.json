{
  "name": "@silo/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "bunx tsc --noEmit",
    "test": "bun test",
    "format": "bunx prettier --check ."
  },
  "dependencies": {
    "@silo/agent-adapters": "workspace:*",
    "@silo/core": "workspace:*",
    "@silo/db": "workspace:*",
    "@silo/gateway": "workspace:*",
    "@silo/git": "workspace:*",
    "@silo/os-adapters": "workspace:*",
    "zod": "^3.24.4"
  }
}
