{
  "name": "@silo/agent-adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "bunx tsc --noEmit",
    "test": "bun test",
    "format": "bunx prettier --check ."
  },
  "dependencies": {
    "@silo/core": "workspace:*"
  }
}
